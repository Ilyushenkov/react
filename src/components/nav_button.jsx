import React from 'react';

const NavButton = (props) => {
    return (
        <div>
            <input type={'button'} className={'button'} value={'Вперед'} />
        </div>
    );
};

export default NavButton;